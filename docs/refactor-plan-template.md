# 重构计划

> 由 refactor-insight 生成。坏味道定位/计数为脚本硬事实，严重度/成本/建议动作为 LLM 判断（标注「推断」）。

生成时间：`__generated_at__` ｜ 扫描范围：`__repo_path__` ｜ 阈值：`__thresholds__`

## 1. 概览摘要
- 仓库：`__repo_name__`（语言 `__language__`）
- 源码文件数：`__scanned_count__` ｜ 检出坏味道项：`__smell_total__`
- 一句话结论：`__summary__`

## 2. 问题清单

| # | 文件:行 | 规则 | 证据 | 严重度(1-5) | 成本(S/M/L) | 建议动作（推断） | 风险 |
|---|---|---|---|---|---|---|---|
| 1 | `src/...:L12` | 超长文件 | 520 行 | 4 | M | 拆分模块 | 低 |

## 3. 优先级排序
- **P0（先做）**：severity 高 + 成本低
- **P1（次之）**：severity 中
- **P2（可延后）**：噪音类（如 TODO 密度）

## 4. 依赖顺序编排
- 结合 `--deps` 的模块依赖关系，列出「必须先重构」的被依赖模块，避免顺序冲突。

## 5. 红线与确认
- [ ] 高风险重构项已人工确认
- [ ] 本计划仅提供建议，未对任何源码做修改

## 附录
- 规则阈值、扫描范围、排除目录、脚本命令与版本、生成时间。