"""Long-function fixture (python). process_pipeline body intentionally > 80 lines for refactor-smell tests."""


def process_pipeline(data):
    acc = 0
    for chunk in data:
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
        acc += chunk
    return acc