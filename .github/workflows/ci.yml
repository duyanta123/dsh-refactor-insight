name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        node: [18, 22]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
      - name: Syntax check (refactor-smell)
        run: node --check scripts/refactor-smell.mjs
      - name: Syntax check (reused scanner)
        run: node --check scripts/arch-profile.mjs
      - name: Syntax check (plugin entry)
        run: node --check plugin/index.js
      - name: Run refactor-smell contract tests
        run: npm test
      - name: Validate package contents
        run: npm pack --dry-run